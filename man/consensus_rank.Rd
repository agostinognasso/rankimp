% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus.R
\name{consensus_rank}
\alias{consensus_rank}
\alias{print.consensus_rank}
\title{Kemeny consensus ranking of variable importance}
\usage{
consensus_rank(
  x,
  weights = NULL,
  algorithm = c("auto", "exact", "quick", "fast", "decor"),
  ties = TRUE
)

\method{print}{consensus_rank}(x, ...)
}
\arguments{
\item{x}{A \code{consensus_rank} object.}

\item{weights}{Optional numeric vector of length \code{nrow(x)} giving the
weight of each judge. Weights let a permutation importance computed
out-of-bag count for more than an impurity-based one. When \code{x} is a
\code{judges} object carrying method weights (see \code{\link[=importance_judges]{importance_judges()}} and
\code{\link[=judge_weights]{judge_weights()}}), those are used unless \code{weights} overrides them.}

\item{algorithm}{One of \code{"auto"}, \code{"exact"}, \code{"quick"}, \code{"fast"},
\code{"decor"}.}

\item{ties}{Keep ties in the consensus ranking.}

\item{...}{Unused.}
}
\value{
An object of class \code{consensus_rank}, a list with elements \code{ranking}
(a tibble of variable and consensus rank), \code{tau} (the average \code{tau_x}
agreement between the consensus and the judges), \code{consensus_all} (every
optimal consensus found, one per row), \code{judges} and \code{weights} (the panel
as supplied), and the settings used.

When several rankings attain the minimum, \code{ranking} holds their combination
rather than an arbitrary one of them: see the section below.

The panel is kept because the consensus alone is a point estimate:
\code{\link[=rank_confsets]{rank_confsets()}} resamples the judges to put an interval around it, and it
cannot do that from a ranking.
}
\description{
Given \code{K} judges, each expressing a ranking over the same \code{p} variables,
returns the median ranking in the sense of Kemeny: the ranking that
minimises the total Kemeny-Snell distance to the judges,
\deqn{\pi^{*} = \arg\min_{\pi} \sum_{k=1}^{K} w_k \, d_{KS}(\pi, \pi_k).}
}
\details{
Ties in the consensus are meaningful and are kept by default. Variables
that the judges genuinely cannot separate \emph{should} come out equal, which
is what distinguishes a Kemeny median from an average of Borda scores.
Set \code{ties = FALSE} to force a linear order.
}
\section{When the median is not unique}{

Several rankings can attain the same minimum, and \code{ConsRank} returns them all.
Reporting one of them would be arbitrary in a way that is not neutral: which
comes first depends on the order of the columns, so a variable can gain a
position by sitting to the left. That was measured — on a symmetric panel,
permuting the columns changed the winner; in a simulation with three
exchangeable noise predictors the leftmost took the best rank systematically,
and the situation is not rare, arising in 57\% to 98\% of replicates there.

The consensus reported is therefore the optimal set combined: each variable
takes its average position over the optima, and those that come out equal are
tied. Variables the objective genuinely cannot separate are reported as
equal, which is the point of taking a median over weak orderings. The whole
set remains in \code{consensus_all}.
}

\section{Choice of algorithm}{

Finding the Kemeny median is NP-hard, so \code{algorithm = "auto"} picks by
problem size:
\itemize{
\item \code{p <= 10} — \code{"exact"}, branch-and-bound.
\item \verb{11 <= p <= 50} — \code{"quick"}.
\item \code{p > 50} — \code{"fast"}, with a message. The integer-programming route of the
roadmap, which would restore optimality guarantees at this size, is a phase
F2 deliverable.
}

The exact threshold is ten rather than the fifteen \code{ConsRank} permits,
because the cost of branch-and-bound is not a smooth function of \code{p} and the
panels this package produces are the hard ones. Importance scores tie: the
unimportant variables all sit near zero and rank equal. On tied panels of
thirty judges the same solver took 0.010 s at \code{p = 10}, 0.78 s at \code{p = 11}
and 280 s at \code{p = 12}. Meanwhile \code{"quick"} returned the identical consensus
and the identical \code{tau_x} on twenty out of twenty tied panels at \code{p = 10}.
Exactness above ten variables buys little and can cost minutes, so ask for it
deliberately with \code{algorithm = "exact"}.
}

\examples{
judges <- rbind(
  c(1, 2, 3, 4),
  c(1, 2, 4, 3),
  c(2, 1, 3, 4)
)
colnames(judges) <- c("income", "age", "balance", "region")
consensus_rank(judges)

}
\seealso{
\code{\link[=importance_to_rank]{importance_to_rank()}}, \code{\link[=rank_confsets]{rank_confsets()}}
}
