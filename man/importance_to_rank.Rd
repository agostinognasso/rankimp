% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/judges.R
\name{importance_to_rank}
\alias{importance_to_rank}
\title{Turn importance scores into rankings}
\usage{
importance_to_rank(x, ties_method = c("min", "average", "first"))
}
\arguments{
\item{x}{Numeric matrix or data frame of importance scores, judges in
rows and variables in columns. Higher is more important.}

\item{ties_method}{Passed to \code{\link[base:rank]{base::rank()}}. The default, \code{"min"},
produces the weak orderings that the Kemeny framework expects.}
}
\value{
An integer matrix of rankings with the same dimensions and
dimnames as \code{x}, suitable for \code{\link[=consensus_rank]{consensus_rank()}}.
}
\description{
Each row of \code{x} holds the importance that one judge assigns to each
variable; the result holds the rank that judge gives each variable, with
\code{1} for the most important. Variables a judge scores equally receive the
same rank, because pretending to separate them would invent information
the judge never supplied.
}
\examples{
imp <- rbind(
  permutation = c(income = 0.31, age = 0.12, balance = 0.12),
  impurity    = c(income = 0.44, age = 0.20, balance = 0.05)
)
importance_to_rank(imp)
}
