% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heterogeneity.R
\name{item_consensus}
\alias{item_consensus}
\title{Agreement of each judge with the consensus}
\usage{
item_consensus(cr)
}
\arguments{
\item{cr}{A \code{consensus_rank} object.}
}
\value{
A tibble with one row per judge: its name (or index), its weight, and
its \code{tau_x} against the consensus, in increasing order of agreement.
}
\description{
The Emond-Mason \code{tau_x} between each judge's ranking and the consensus.
\code{cr$tau} is the (weighted) mean of this column; the column itself says
whether that mean summarises a panel that agrees or averages a panel that is
split, which are different situations reported by the same number.
}
\details{
A judge with a \code{tau_x} near zero is not necessarily wrong. Marginal and
conditional importance measures disagree by construction when the predictors
are correlated, and one of them will look like an outlier against a panel
dominated by the other.
}
\examples{
judges <- rbind(
  permutation = c(1, 2, 3, 4),
  shap        = c(1, 3, 2, 4),
  impurity    = c(4, 3, 2, 1)
)
colnames(judges) <- c("income", "age", "balance", "region")
item_consensus(consensus_rank(judges))
}
\seealso{
\code{\link[=judge_clusters]{judge_clusters()}} for what to do when the agreement is low.
}
